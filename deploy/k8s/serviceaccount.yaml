apiVersion: v1
kind: ServiceAccount
metadata:
  name: victor
  namespace: victor
  annotations:
    # Azure Workload Identity — replace with the managed identity client ID
    azure.workload.identity/client-id: "<AZURE_CLIENT_ID>"
